# To-Do

## High Priority

- [ ] Fix order by `<field>` desc not working as we haven't doubly linked the B+Tree leaves yet.
- [ ] Add something similar to `if not exists` for `make table` statements, currently this is the default behaviour, but it should be error by default and optional to skip if exists.
- [ ] Query Parameters !!!